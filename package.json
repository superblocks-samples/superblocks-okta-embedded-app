{
  "name": "okta-superblocks-embed",
  "version": "1.0.0",
  "description": "A repository for integrating Superblocks with custom authentication using Okta.",
  "scripts": {
    "install": "npm install --prefix ./app && npm install --prefix ./lambda",
    "start": "npm run dev --prefix ./app",
    "test:lambda": "npm test --prefix ./lambda",
    "build:app": "npm run build --prefix ./app",
    "build:lambda": "npm run build --prefix ./lambda",
    "build:all": "npm run build --prefix ./app && npm run build --prefix ./lambda"
  },
  "author": "Kris Harbold",
  "license": "MIT"
}
