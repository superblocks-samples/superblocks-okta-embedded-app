{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "sam:build": "sam build",
    "sam:local": "sam local start-api --env-vars env.local.json --port 3001",
    "sam:invoke": "sam local invoke TokenExchangeFunction --env-vars env.local.json -e events/token-exchange.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@okta/jwt-verifier": "^4.0.1",
    "axios": "^1.6.7",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
