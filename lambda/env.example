# ============================================================================
# Lambda Token Exchange Function - Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env file with real credentials to version control
# ============================================================================

# ----------------------------------------------------------------------------
# Required Configuration
# ----------------------------------------------------------------------------

# Superblocks Embed Access Token
# This token authenticates the Lambda function with the Superblocks Session API
# Get this from: Superblocks Admin > Settings > Access Tokens
# Create a new token with "Embed" permissions
SUPERBLOCKS_EMBED_ACCESS_TOKEN=sb_embed_your-token-here

# Okta Authorization Server Issuer URL
# Must match REACT_APP_OKTA_ISSUER in the React app
# Format: https://{OKTA_DOMAIN}/oauth2/default
# Example: https://dev-12345.okta.com/oauth2/default
OKTA_ISSUER=https://your-domain.okta.com/oauth2/default

# Your Superblocks instance URL (with https://)
# Must match REACT_APP_SUPERBLOCKS_URL in the React app
# Example: https://your-instance.superblocks.com
SUPERBLOCKS_URL=https://your-instance.superblocks.com

# ----------------------------------------------------------------------------
# Optional Configuration
# ----------------------------------------------------------------------------

# Okta Audience (aud claim)
# The expected audience value for JWT validation
# Default: api://default
# Set this to match your Okta Authorization Server audience configuration
# OKTA_AUDIENCE=api://default

# Debug Mode (enables verbose logging with sensitive data)
# WARNING: Only enable in local development - logs will include user data and tokens
# Set to 'true' for local development, leave unset or 'false' for production
# DEBUG=true

# Local Development Server Port
# Only used when running the dev server locally (npm run dev)
# Default: 3001
# PORT=3001
